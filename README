This is a Django and Javascript application to provide integration between Gmail
and Poligraft, initially via Rapportive, but potentially also via bookmarklet
and/or browser extension.

Installation
============

To install:

    pip install -e git+http://github.com/sunlightlabs/oxtail.git#egg=oxtail

